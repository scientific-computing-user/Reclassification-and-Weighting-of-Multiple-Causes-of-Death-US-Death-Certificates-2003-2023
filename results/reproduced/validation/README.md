# `results/reproduced/validation/`

Validation copies written during a `reproduce_snapshot.sh` run.

## Typical outputs

- `claim_check.md`
- `claim_metrics.json`

These are run artifacts; canonical package-level audit outputs live in `results/validation/`.
